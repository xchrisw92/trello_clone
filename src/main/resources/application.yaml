
spring:
  application:
    name: trello_clone
  datasource:
    url: jdbc:postgresql://localhost:5434/trello_clone_database
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
